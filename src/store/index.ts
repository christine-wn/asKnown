import { createStore } from 'vuex'
import { testData } from './testData'

export default createStore({
  state: {
    testData: testData,
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
